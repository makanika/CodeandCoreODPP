# ODPP Progress Check

**Checked:** 13:10, 1 September 2026  
**Evidence:** Local Django project and `https://odpp-complaints.codeandcore.dev`  
**Status:** Core complaint intake and protected staff workflow are functioning; the POC is not yet complete against `brief.md`.

## Verified Working Behaviour

| Area | Position | Evidence |
| --- | --- | --- |
| Two-door entry | Working | Live home page separates officer sign-in from the public complaint centre. |
| Public boundary | Working in the checked routes | Public hub has no case search or staff directory. The tracker requires a complaint reference and six-digit PIN. |
| Tracking anti-enumeration | Working in the checked failure path | Invalid reference/PIN returns one generic mismatch message and did not expose sampled case references, officer names, or complaint narrative. |
| Staff boundary | Working in the checked routes | Staff complaint queue redirects to sign-in when unauthenticated. |
| Conduct boundary | Working for unauthenticated access | Direct anonymous access to `/conduct/` returns a generic `403`. |
| Guided complaint intake | Working locally | Current local check records case lookup, NIN verification, and guided lodging using the same receipt pipeline as public intake. |
| Case, complaint, staff connection | Working locally | `cases` has four applied migrations; `complaints` has four; staff profiles, scoped queues, complaint detail, and linked case context are implemented. |
| Type A persistence boundary | Partially evidenced | `conduct` now has `0001_initial` on the isolated database. Its models use identity snapshots rather than primary-database foreign keys. Subject-officer recusal and audit tests remain required. |

## Live Alignment Gaps Against The Brief

| Brief requirement | Current position | Required next action |
| --- | --- | --- |
| `/offices/` public office directory | Missing: live route returns `404`. | Build the approved office directory and assistance/contact route. |
| Privacy and lawful-processing statement on public intake | Not visible in the checked public hub or form. | Add approved privacy, lawful-processing, retention, and accessible-assistance content before production, especially because NIN is collected. |
| Public evidence upload | Missing from the checked public form. | Define permitted types/size, private storage, malware scanning, retention, and Type B-only linking before enabling uploads. |
| Staff-facing document upload | Missing locally: `documents/views.py` does not exist. | Build the restricted upload, scan-status, and permitted-download workflow. |
| `/pipeline/` staff case timeline | Missing: live route returns `404`. | Build a scoped complaint-context case folder and custody timeline. Do not add general prosecution case management. |
| `/insight/latency/` executive analytics | Missing: live route returns `404`; `insight` has no migration. | Build the internal analytics snapshot/reporting foundation next after uploads or in parallel with it. |
| Rate limits on public submission and tracking | Not demonstrated by the live review. | Add and test throttling before any production exposure. |
| Type A recusal/audit proof | Not demonstrated by the live review. | Test list, detail, search, export, and download denial for a subject officer; write allowed/denied audit records. |

## Corrected Route Interpretation

The direct request to `/complain/done/` returned `404`. This is **not** recorded as a functional defect: the receipt endpoint is intended to be one-time and session-bound, so a user without a freshly created complaint receipt should not be able to retrieve it by guessing a URL. The local test record shows the receipt works after the normal submission flow.

## Applied Migration Position

- `accounts.0001_initial`
- `common.0001_initial`
- `staff.0001_initial`
- `documents.0001_initial`
- `cases.0001_initial` through `cases.0004_caseparty`
- `complaints.0001_initial` through `complaints.0004_complaint_stakeholder_role`
- `conduct.0001_initial`
- `insight`: no migration; still an empty model stub.

## Direction: Finish Operational Evidence, Then Insight

1. Build the restricted staff document-upload workflow first. It must enforce type/size validation, private storage, processing status, complaint/case linkage, author attribution, and download audit data.
2. Build the scoped case-folder/pipeline view at `/pipeline/`, limited to complaint-context case identity, custody movement, permitted documents, linked Type B indicators, and audit history.
3. Build `insight` as an internal snapshot/reporting service: 14/30-day delay bands, regional comparison, queue age, escalation pressure, reassignment volume, and aggregate conduct counts only.
4. Add `/offices/`, public privacy/lawful-processing copy, and approved assistance guidance before public pilot review.
5. Add rate-limit, scope-denial, Type A recusal, upload/download, and PDF/export filtering tests before classifying the POC as ready for UAT.

## Outstanding Approval Decisions

The official station-to-region map, action/scope matrix, Type A handoff and reclassification policy, document taxonomy/redaction rules, retention schedule, public contact wording, and production integration providers still require ODPP approval.