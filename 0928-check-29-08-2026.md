# ODPP Progress Check

**Checked:** 09:28, 29 August 2026
**Status:** DPP directorate workspace built end-to-end (staff directory, workload drill-down, assign/comment/move-file); four real bugs found and fixed along the way. Nothing broken — full check, test suite, and a role-based smoke test all pass. Work is uncommitted on `main`.

## Current Position

| Area | Status | Verified position |
| --- | --- | --- |
| Django environment | Ready | `manage.py check` passes; all migrations applied, including two new ones (`cases.0002_casecomment`, `complaints.0002_alter_complaintevent_event_type`). |
| DPP identity | Ready | Director of Public Prosecutions is seeded as **Linus Anguzu**, logs in as `linus.anguzu@odpp.local` / `Odpp@254`. Confirmed via test-client login against `/dashboard/` and `/staff/profile/`. |
| Director architecture decision | Resolved | DPP and Head of Complaints do **not** have their own app. They're a permission tier (`staff.permissions.is_director`) over the existing `dashboard`/`complaints`/`cases` apps, not a new domain. |
| Staff directory | Built | `/dashboard/staff/` — org-wide staff list with live workload counts, gated to director-tier roles (403 for everyone else, verified with the RSA account). |
| Staff workload drill-down | Built | `/dashboard/staff/<id>/` — identity, reporting line, direct reports, active complaints and allocated cases. |
| Assign / comment / move-file | Built | `cases.services.assign_case` and `move_case` added (mirroring the existing `complaints.services.assign_complaint` pattern) — `CaseMovement` previously had no live code path writing to it outside the seed script. Complaint comments post into the existing `ComplaintEvent` timeline (new `COMMENT` event type); case comments use a new `CaseComment` model, since cases had no remark capability at all before. All three actions smoke-tested as Linus (succeed) and as an RSA (assign/move correctly blocked with a message, comment allowed). |
| Test suite | Same as last check | Still only 2 real tests (`documents`, `complaints`); `staff`/`cases`/`common`/`accounts` remain stock boilerplate. Not addressed this session — flagged again, not fixed. |

## Problems Found and Fixed

| Problem | Cause | Fix applied |
| --- | --- | --- |
| `manage.py test` misdiscovered/crashed across apps (flagged last check) | `staff`, `documents`, `insight` had no `__init__.py`, loading as implicit namespace packages. | Added the three empty `__init__.py` files. Confirmed: full suite now correctly finds exactly 2 tests. |
| `seed_demo_cases` crashed on a fresh database | `_upsert_assignment` passed `ended_at__isnull=True` into `update_or_create`, which is a valid filter but not a valid `.create()` kwarg. | Replaced with an explicit filter-then-create. |
| Re-running `seed_demo_staff` crashed with a `UNIQUE constraint failed: officer_number` | A prior session had manually renamed the RSA demo account's username (to `amina.kato@odpp.local`) without updating the seed script, so re-seeding tried to create a second account and collided. | Updated the seed script to match the live username; also fixed the email-generation line, which would have produced a malformed double-`@` address for any username already containing `@`. |
| Every `LoginRequiredMixin` page (dashboard, staff profile, and the new director pages) sent a logged-out visitor to a 404 | `LOGIN_URL` was never set in `settings.py`, so Django defaulted to `/accounts/login/`, which doesn't exist in this project — the real sign-in view is `/` (`staff-login`). | Set `LOGIN_URL = 'staff-login'`. Verified: anonymous `/dashboard/` now 302s to `/?next=/dashboard/`. |

## Direction: Next Slice

1. Commit this work — nothing has been committed yet; `git status` still shows the full diff pending.
2. Real test coverage for `cases`, `staff`, `common`, `accounts` — currently only `documents` and `complaints` have anything beyond Django boilerplate, and today's new services (`assign_case`, `move_case`, `add_complaint_comment`) have no regression tests, only the manual smoke test run this session.
3. SLA breach handling — `overdue_count` is still display-only; no automatic escalation or notification exists when a complaint passes its SLA date.
4. Type A conduct schema — still not started; unchanged from every prior check.

## Outstanding Decisions

Unchanged: the action/scope matrix, official station-to-region map, and final Type A handoff policy still require ODPP approval. Continue using controlled `DEMO` values until those are signed off.
