# ODPP Progress Check

**Checked:** 09:04, 29 August 2026
**Status:** Type B complaints workflow and its staff workspace are functionally built and seeded; a real bug and one open scope decision need attention before going further.

## Current Position

| Area | Status | Verified position |
| --- | --- | --- |
| Django environment | Ready | Django `5.2.8`; `manage.py check` passes; all migrations applied on `default` (`accounts`, `common`, `staff`, `documents`, `cases`, `complaints` all `[X]`). |
| Accounts, staff, offices | Ready | 12 seeded `DEMO` staff profiles with postings, reporting lines, and scope assignments (carried over from the last check). |
| Documents | Foundation ready | Controlled document metadata, processing status, and logged-in-author comment attribution; unchanged since last check. |
| Case context | Built | `cases.CaseReference`, `CaseIdentifier`, `CaseMovement`, `CaseAssignment`, `CaseDocumentLink` exist and are migrated. 9 demo cases seeded via `seed_demo_cases`. Deliberately limited to reference/custody tracking, not prosecution workflow. |
| Type B complaints | Built | `complaints` models (`Complaint`, `ComplaintAssignment`, `ComplaintInquiry`, `ComplaintDetermination`, `ComplaintCommunication`, `ComplaintDocument`, `ComplaintEvent`, `FileRecallOrder`) and `services.py` state machine are implemented and migrated. 7 demo complaints seeded via `seed_demo_complaints`. |
| Public portal | Built | `complain/`, `complain/done/`, `track/` routes lodge a complaint, hash a 6-digit PIN, issue a QR receipt, and support reference+PIN tracking without exposing internal status detail. |
| Staff workspace | Built | `dashboard/` shows a role-scoped operational view (SLA counts, overdue/due-soon, workload trend); `dashboard/complaints/<pk>/` shows a scoped complaint detail with events, inquiries, determinations, communications, and linked documents. Visibility is enforced through `complaints.services.visible_complaints_for`, scoped by office/region/role, not a blanket staff view. |
| Type A conduct | Not started | `conduct` app and database still have no schema. Confirmed unchanged. |
| Test suite | Partially blocked | See defect below — full `manage.py test` only discovers 1 test; per-app discovery fails outright. |

## Problems Found (New)

| Problem | Cause | Fix needed |
| --- | --- | --- |
| `manage.py test` silently runs only 1 test instead of the suite across `accounts`, `documents`, `complaints`, etc. Running per-app (`manage.py test staff documents ...`) throws `ImportError: 'tests' module incorrectly imported from 'staff'. Expected 'documents'.` | `staff`, `documents`, and `insight` are missing `__init__.py`, so they load as implicit namespace packages. Python's `unittest` file-based test loader cannot disambiguate two namespace packages that each contain a top-level `tests` module, so it either merges/misresolves them or errors. | Add an empty `__init__.py` to `staff/`, `documents/`, and `insight/` (matching every other app). This is a straightforward, low-risk fix and should happen before more app-level tests are written, since coverage is currently not trustworthy. |

Not yet fixed — flagging for your call, not applied automatically since it touches every app.

## Boundary Check

Reviewed `cases` and `complaints` again against the stated boundary: `cases` still only carries reference identifiers, movement/custody, and assignment — no charge drafting, trial prep, or court scheduling. `complaints.services.handoff_type_a` deliberately strips subject/narrative before marking a complaint as transferred, so sealed conduct content does not leak into the ordinary database. This holds up.

## Direction: Next Slice

1. Fix the missing `__init__.py` in `staff`, `documents`, `insight`, then confirm `manage.py test` runs the full suite (expect more than 1 test to be discovered).
2. Wire escalation actions (assign, transition, determine, communicate) into the staff complaint-detail view — the service functions exist (`assign_complaint`, `transition_type_b`, `classify_type_b`) but are not yet called from any view/form.
3. Add SLA breach handling — `sla_due_at` and `overdue_count` are computed for display, but there is no automatic escalation action or notification when a complaint goes overdue.
4. Build the sealed Type A conduct schema, using `handoff_type_a` as the entry point, only after item 2 is working end to end.

## Outstanding Decisions

Unchanged: the action/scope matrix, official station-to-region map, and final Type A handoff policy still require ODPP approval. Continue using controlled `DEMO` values until those are signed off.
