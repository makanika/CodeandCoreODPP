# ODPP Progress Check

**Checked:** 06:15, 29 August 2026  
**Workspace status:** Django foundation scaffold created; configuration not yet adapted to the ODPP design.

## Progress Since Last Check

- The Django project is now present with `manage.py` and the `core` configuration package.
- Application folders now exist for `accounts`, `cases`, `complaints`, `conduct`, and `common`.
- Django has been aligned with the dependency file: the isolated `.env/` environment now runs `Django 5.2.8`.
- `python manage.py check` passes with zero reported issues.

## Current Readiness

| Area | Status | Direction |
| --- | --- | --- |
| Python environment | Ready | Python `3.14.6`; pinned dependencies installed and resolving. |
| Django version | Ready | Django `5.2.8` LTS is installed and supports Python `3.14`. |
| Django scaffold | Started | Project and application folders exist. |
| Application registration | Not started | The ODPP applications are not in `INSTALLED_APPS`. |
| Custom staff user | Not started | Must be defined and configured before the first migration. |
| Database separation | Not started | `core/settings.py` still uses one SQLite database; the brief requires primary and conduct database routing. |
| Secrets and environment settings | Not started | The generated development `SECRET_KEY` remains in `core/settings.py`; move it to environment configuration before shared use. |
| URLs and templates | Not started | Only the stock Django admin route exists. |
| Migrations and tests | Not started | No application migrations exist. |

## Direction: Next Build Slice

Do the following in order. Do not create or apply migrations until steps 1 and 2 are complete.

1. Correct the application inventory: rename `insignt` to `insight` to match the agreed architecture; confirm whether the additional `dashboard` app is intentional or fold its eventual views into the role-based dashboard surface.
2. Configure the foundation in `core/settings.py`: register the agreed applications, configure `AUTH_USER_MODEL`, set `Africa/Kampala`, define template/static locations, and load secrets/settings through `django-environ`.
3. Define `accounts.StaffUser` as the custom user model with the strict role enum and explicit scope relationships required by the brief. This must land before the first migration.
4. Add the primary/conduct PostgreSQL configuration, database router, and a local development strategy that keeps sealed conduct data physically separate. Do not implement Type A narrative fields in the primary database.
5. Create the first migrations and tests for the custom user model, scope enforcement, and append-only audit events. Run `manage.py check` and the focused test suite after each slice.

## Decision Needed Before Step 4

Confirm the local development database approach: two local PostgreSQL databases are preferred because the conduct isolation requirement cannot be meaningfully proven with a single SQLite database. The station-to-region map, action matrix, and Type A sanitised handoff fields also remain approval-dependent, but the custom user and security foundation can proceed while those are documented as controlled values.