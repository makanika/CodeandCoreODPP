# ODPP Progress Check

**Checked:** 12:46, 31 August 2026
**Workspace status:** Clean Git worktree — all changes committed as `858bac8 added_public_complaints`.
**Status:** Built the public-facing complaint intake system end to end (officer-guided desk flow, public landing/hub, complaints queue) after a live demo surfaced navigation and polish gaps. Fixed two real defects found along the way — fabricated placeholder identities and mislabelled event history — and replaced raw-text evidence documents with rendered PDFs.

## Session Context

This session followed a live presentation. Feedback from that demo drove the work directly: no direct route to a complaints list (had to go Staff → workload → cases), fabricated "DEMO"-labelled names visible on screen, and evidence documents opening as unstyled raw text. Each is addressed below.

## Built This Session

| Area | What changed | Verified |
| --- | --- | --- |
| Officer-guided complaint intake | New `CaseParty` model (`cases`) recording a case's real stakeholders (name, NIN, phone, role). Three new staff views — case lookup, NIN verification, guided lodge — reuse the existing `create_complaint`/receipt pipeline rather than duplicating it. | Full HTTP walk-through via the Django test client against real seed data: lookup → wrong NIN correctly rejected → correct NIN verified → lodge → redirected to the branded receipt page. |
| Public entry & branding | Root `/` is now the two-door landing page from brief.md §6.1 (Officer sign-in / Public complaint centre) instead of dropping straight into the staff login form; staff login moved to `/staff/sign-in/`. New `/complaints/` public hub. `lodge.html`, `track.html`, `receipt.html` — which had no institutional header at all — now carry the full ODPP topbar and masthead. | All public and staff routes smoke-tested: 200 across `/`, `/complaints/`, `/complain/`, `/track/`, `/staff/sign-in/`, `/dashboard/`, `/dashboard/complaints/`, `/desk/complaints/lookup/`. |
| Complaints queue | New `/dashboard/complaints/` list view (status-filterable, scope-respecting via the existing `visible_complaints_for`) plus a "Complaints" link in the staff nav. | Closes the exact gap reported live: no more Staff directory → workload → cases detour to find a complaint. |
| Seed data integrity | Removed fabricated `DEMO Complainant`/`DEMO Accused` placeholder rows (flagged as amateur-looking mid-demo). `CaseParty` now derives from the 5 already-seeded complaints' real complainant data (Grace Namuli, Robert Ssekandi, Fatuma Nakirya, Ivan Bwambale, Sarah Achieng), linked to the case each actually complained about. | Confirmed zero `demo`-labelled names remain; 5 `CaseParty` rows, each traceable to a real `Complaint`. |
| Document rendering | Replaced raw `.txt` evidence/intake/determination files (opened as unstyled plain text) with letterhead PDFs (`documents/services.py`, built on `reportlab` — pure-Python, no system dependencies). First pass was too thin (1–3 sentences); expanded to pull real relational data instead of padding: station diary extracts now include the full chain of custody from actual `CaseMovement` rows, intake forms include the full `ComplaintEvent` processing timeline, determination letters pull the real `ComplaintCommunication` record. | Rendered and visually inspected against live seed data; confirmed 0 `.txt` / 16 `.pdf` documents remain, all served as `application/pdf`. |
| Bug fix (found while building the above) | `transition_type_b` was mislabelling every "inquiry opened" event as "Determination recorded" — an event-type heuristic that only distinguished `ESCALATED` from everything else, now visible for the first time once event history started appearing in real documents. | Fixed in `complaints/services.py`; corrected the 5 existing event rows (the 2 already sealed into Type A conduct were left untouched). |

## App Connectivity Audit

| App | Status |
| --- | --- |
| `accounts` | Connected — auth, plus the new two-door landing page. |
| `staff` | Connected — profiles/postings/scope. |
| `common` | Connected — Office/Region reference data. |
| `cases` | Connected — extended this session with `CaseParty`. |
| `complaints` | Connected — extended this session with guided desk intake, the public hub, and the complaints queue. |
| `documents` | Partially connected — now renders real letterhead PDFs, but **still no staff-facing upload form**; documents can only be created via the seed command or Django admin. Same gap flagged in the 18:17 (29 Aug) check, unchanged since. |
| `dashboard` | Connected — added the complaints queue. |
| `conduct` | Connected — has models, views, and the Type A handoff flow wired in. (Was an empty stub as of the 18:17, 29 Aug check; built out since, not part of this session's work.) |
| `insight` | **Still not connected** — empty stub, unchanged. No analytics/reporting exists yet against brief.md §8 (14/30-day latency bands, regional comparisons, escalation flags). |

## Direction: Next Slice

1. `documents`: build the staff-facing upload form — the one remaining gap in an app that now at least renders correctly.
2. `insight`: the only fully untouched stub left; decide whether analytics/reporting is the next body of work.
3. Test coverage remains thin — only `complaints` (34 lines) and `documents` (42 lines) have real tests. Today's `CaseParty` verification logic, PDF rendering, and the `transition_type_b` fix have manual/live verification only, no regression tests.
4. Open question from this session, not yet resolved: whether NIN-based stakeholder verification needs a supervisor-override path for cases where a legitimate stakeholder wasn't captured at case intake — currently the officer is fully blocked with no fallback.

## Outstanding Decisions

Unchanged from prior checks: the role/action/scope matrix, official station-to-region mapping, and final Type A handoff policy still require ODPP approval before production.
